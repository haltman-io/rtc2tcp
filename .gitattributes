# Force LF line endings on POSIX shell scripts and service files so they
# run correctly when checked out on Linux after being authored/edited on
# Windows.
*.sh            text eol=lf
*.service       text eol=lf
*.env           text eol=lf
*.env.example   text eol=lf
